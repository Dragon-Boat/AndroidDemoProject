<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sundae.zl.androiddemoproject">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".recyclerview.RecyclerViewDemoActivity" />
        <activity android:name=".FActivity" />
        <activity android:name=".vlayoutexample.MainActivity" />
        <activity android:name=".vlayoutexample.OnePlusNLayoutActivity" />
        <activity android:name=".vlayoutexample.RootActivity" >
        </activity>
        <activity android:name=".vlayoutexample.TestActivity" />
        <activity android:name=".vlayoutexample.VLayoutActivity" />
        <activity android:name=".WebViewActivity" />
    </application>

</manifest>